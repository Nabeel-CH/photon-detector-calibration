# S1 Coursework

Repository for the S1 coursework. 
The solution for this project is in the Jupyter Notebook `notebooks/solution.ipynb`.

The assignment question is provided in `notebooks/instructions.ipynb`.

The majority of the code functionality is implemented in the `s1_sol` package:
- `s1_sol/s1funcs.py` contains functions for fitting, model definitions and utilities etc 
- `s1_sol/s1plots.py` contains functions for generating plots

The solution notebook produces:
- Figures saved in the `figs/`
- A `results.json` file containing parameter estimates from different methods

## Installation

1. Clone this GitLab repository to your local machine:
   ```bash
   git clone <repository-url>
   ```

3. Create and activate a virtual environment (recommended):
   ```bash
   python -m venv venv
   source venv/bin/activate  
   ```

2. Install the package and its dependencies:
   ```bash
   pip install -e .
   ```
   
   This will install the `s1_sol` package.

4. If needed, create a Jupyter kernel for this environment (this will require ipykernel):
   ```python
   python -m ipykernel install --user --name s1_coursework --display-name "S1 Coursework"
   ```

5. Open the solution notebook.

You should now be able to run the notebook. 

## Project Structure

```
S1/
├── LICENSE
├── pyproject.toml           
├── results.json               # Contains parameter results from the 3 methods
├── sample.csv                 # Data file
├── figs/                      # Figures generated by the solution notebook
├── notebooks/                 # Jupyter notebooks
│   ├── instructions.ipynb     # Coursework instructions
│   └── solution.ipynb         # My solution
├── s1_sol/                    # Solution package
│   ├── example.py
│   ├── s1funcs.py             # Fitting functions, model defs, utilities etc
│   ├── s1plots.py             # Function for plots
└── README.md                  # This file
```

## Declaration of Use of Autogeneration Tools

Copilot was used in the following: 
 - Copilot auto suggestions were used for assiting in: writing repetitive codes, codes auto suggestions identified patterns, assitsted in docstrings and comments to code.
 - Copilot was used to generate the tree project sturcture above 

 ChatGPT was used in the following:
 - My initial bootstrap loop was very slow, so I asked GPT for suggestions on way to optimise the code for efficiency, I then implemented this manually.
